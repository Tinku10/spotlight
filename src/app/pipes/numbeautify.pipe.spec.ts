import { NumbeautifyPipe } from './numbeautify.pipe';

describe('NumbeautifyPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbeautifyPipe();
    expect(pipe).toBeTruthy();
  });
});
